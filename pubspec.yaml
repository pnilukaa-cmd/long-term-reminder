name: long_term_reminder
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.13.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # --- Vertical-slice-1 additions (local storage) ---
  # Versions below are my best estimate of current, well-maintained releases
  # — I have no network access in this environment to check pub.dev, so
  # treat these as a starting point, not a verified pin. Run
  # `flutter pub outdated` after `flutter pub get` to confirm nothing has
  # moved on since.
  drift: ^2.21.0
  sqlite3_flutter_libs: ^0.5.24
  path_provider: ^2.1.4
  path: ^1.9.0

  # --- This slice's additions (notification scheduling) ---
  # Same caveat as the block above, stated again because it matters more
  # here: I have no network access to pub.dev and no Flutter SDK in this
  # environment, so nothing below has been resolved, fetched, or built
  # against. Treat every version as a starting guess, not a verified pin —
  # run `flutter pub outdated` immediately after `flutter pub get` and
  # check each package's current changelog for Android 14/15 (API 34/35)
  # compatibility before trusting it, per
  # docs/technical/04-scheduling-and-stack.md §3's own named risk
  # ("flutter_local_notifications has, historically, had real
  # compatibility bumps around major Android permission-model changes").
  #
  # flutter_local_notifications: the `zonedSchedule` API + `AndroidScheduleMode`
  # enum this slice depends on (inexactAllowWhileIdle specifically) has
  # existed since roughly v9; pinning to a recent major as a starting
  # point.
  flutter_local_notifications: ^18.0.1
  # workmanager: the periodic-reconciliation background task (§2 of the
  # scheduling ADR). Its Dart API (`Workmanager().initialize` /
  # `registerPeriodicTask`, and the top-level `callbackDispatcher`
  # contract) has been stable across recent majors, but this plugin in
  # particular has had real gaps trailing new Android versions historically
  # — verify current maintenance status before relying on it for a ship
  # date.
  workmanager: ^0.6.0
  # timezone: required by flutter_local_notifications' zonedSchedule (it
  # takes a `TZDateTime`, not a plain `DateTime`) — this is the documented
  # mechanism for REQ-17.2's "reminder fires on the correct calendar day
  # relative to the device's *current* timezone," not a separate choice.
  timezone: ^0.9.4
  # flutter_timezone: reads the device's current IANA timezone name so
  # `tz.setLocalLocation` can be re-set on every reconciliation pass —
  # this is what lets a timezone change actually take effect rather than
  # freezing to whatever zone was active at first launch.
  flutter_timezone: ^1.0.8
  # permission_handler: added this slice, specifically for REQ-11.1's
  # "Open notification settings" deep link on denial
  # (`ph.openAppSettings()`) — `flutter_local_notifications`' own
  # `requestNotificationsPermission()` covers the Android 13+ request
  # itself, but has no equivalent for opening the OS settings page.
  # Same unverified-version caveat as every other addition in this block.
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # --- Vertical-slice-1 additions (drift code generation) ---
  drift_dev: ^2.21.0
  build_runner: ^2.4.13

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
